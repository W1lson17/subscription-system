# Este archivo es solo orientativo.
# Crea los archivos .env correspondientes en cada subcarpeta:
#
# backend/.env  → copia de backend/.env.example
# frontend/.env → copia de frontend/.env.example
#
# Para desarrollo:
#   1. cp backend/.env.example backend/.env
#   2. cp frontend/.env.example frontend/.env
#   3. docker-compose up -d
#   4. cd backend && pnpm dev